# Replit – minimal deps to avoid build timeout. No opencv/mediapipe/moviepy.
# Observer uses Gemini-only when cv2 missing; compose endpoint needs moviepy (use worker for that).
numpy==1.26.4
pillow>=10.0.0,<11
tqdm>=4.65.0
requests>=2.28.0
python-dotenv>=1.0.0
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
redis>=5.0.0
boto3>=1.34.0
google-genai>=0.3.0
pandas>=2.0.0
# psycopg2-binary omitted for Replit – use SQLite (DATABASE_URL=sqlite:///./local.db).
# Add psycopg2-binary if using PostgreSQL.
